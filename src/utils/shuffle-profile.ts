import { TProfileInfos } from "../constants/profile-infos";

export function shuffleProfile(array: TProfileInfos[]) {
  for (let i = array.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));

    [array[i], array[j]] = [array[j], array[i]];
  }
  return array;
}
